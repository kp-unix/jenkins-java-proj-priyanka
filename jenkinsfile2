pipeline {
    agent any
 stages {
        stage('Checkout') {
             steps {
           sh """
           echo "Checkout done - $PWD"
           ls -l
           """
            }
        }
         stage('Builiding the Checkout') {
             steps {
           sh """
           echo "-------Building the Java app-------"
           """
            }
        }
}
}
