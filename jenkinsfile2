pipeline {
    agent any
 stages {
        stage('Checkout') {
             steps {
           sh """
           echo "Checkout done - $PWD"
           ls -l
           """
            }
        }
}
}
